{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "spago build -u '+RTS -N2 -RTS'",
    "test": "spago build && jest"
  },
  "devDependencies": {
    "jest": "^24.0.0"
  }
}
